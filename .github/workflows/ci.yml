# TAKTKRONE-I CI Pipeline
# Runs on pull requests and pushes to main

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  PYTHON_VERSION: "3.10"
  PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff

      - name: Run Ruff
        run: ruff check occlm/schemas occlm/ingestion/__init__.py occlm/ingestion/adapters occlm/normalization occlm/storage occlm/synthesis

      - name: Check formatting
        run: ruff format --check occlm/schemas occlm/ingestion/__init__.py occlm/ingestion/adapters occlm/normalization occlm/storage occlm/synthesis

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mypy pydantic types-requests types-PyYAML

      - name: Run mypy
        run: mypy occlm/schemas occlm/ingestion/__init__.py occlm/ingestion/adapters occlm/normalization occlm/storage occlm/synthesis --ignore-missing-imports

  test:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov pydantic httpx

      - name: Run tests
        run: python -m pytest -p pytest_cov tests/ -v --cov=occlm --cov-report=xml --ignore=tests/load --ignore=tests/regression

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: coverage.xml
          fail_ci_if_error: false

  schema-validation:
    name: Schema Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema pyyaml

      - name: Validate JSON schemas
        run: |
          python -c "
          import json
          from pathlib import Path

          schema_dir = Path('data_contracts')
          for schema_file in schema_dir.glob('*.schema.json'):
              print(f'Validating {schema_file}')
              with open(schema_file) as f:
                  schema = json.load(f)
              # Basic validation that schema is valid JSON
              assert 'type' in schema or 'properties' in schema
          print('All schemas valid')
          "

  build:
    name: Build Package
    runs-on: ubuntu-latest
    needs: [lint, typecheck, test]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Check package
        run: twine check dist/*

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
